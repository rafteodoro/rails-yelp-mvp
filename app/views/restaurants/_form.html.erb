<div class="container">
  <h5>Add a review</h5>
  <%= simple_form_for([restaurant, @review]) do |f| %>
    <%= f.text_area :content %>
    <%= f.input :rating %>
    <%= f.button :submit, class:"btn btn-primary" %>
  <% end %>
</div>
